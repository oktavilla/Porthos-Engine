<div class="viewable">
  <h2><%= datum.label %></h2>
  <div class="content">
    <%= datum.allow_rich_text? ? datum.value.to_s.html_safe : simple_format(datum.value) %>
  </div>
</div>
<div class="editable">
  <%= f.label :value, datum.label, :for => "datum_#{datum.id}_value" %>
  <div class="fullwidth">
  <% if datum.multiline? %>
    <%= f.text_area :value, :class => (datum.allow_rich_text? ? 'editor' : 'normal'), :id => "datum_#{datum.id}_value" %>
  <% else %>
    <%= f.text_field :value, :id => "datum_#{datum.id}_value" %>
  <% end %>
  <% if datum.instruction_body %>
    <p class="form_help"><%= datum.instruction_body %></p>
  <% end %>
  </div>
  <% unless datum._parent_document.is_a?(FieldSet) %>
  <div class="submit">
    <%= f.submit t(:save) %>
  </div>
  <% end %>
</div>