<span class="title"><%= link_to taggable.title, admin_page_path(taggable) %></span>
<% unless taggable.full_uri.blank? -%>
<span class="uri">
  /<%= taggable.full_uri %>
</span>
<% end -%>
<span class="meta">
  <% if taggable.updated_at && taggable.updated_at != taggable.published_on -%>
  Senast ändrad <%= taggable.updated_at.strftime("%d %b %Y %H:%M") %>.
  <% end -%>
  <% if taggable.published_on.present? %>
  Publicerad <%= taggable.published_on.strftime("%d %b %Y %H:%M") %>.
  <% end %>
</span>
