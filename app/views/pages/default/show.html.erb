<% @page_class = "article layout2" -%>
<% @page_title = @renderer.page.title -%>
<% @page_id = 'pages-show' -%>
<div id="content">
  <div id="c_1">
    <nav class="sub">
    <%= render :partial => 'pages/contents/modules/sub_navigation/content' %>
    </nav>
  </div>
  <div id="c_2">
    <article>
      <h1><%= @renderer.page.title %></h1>
      <%= @renderer.page.body %>
      <% @renderer.page.contents.active.each do |content| -%>
        <%= render_page_content(@renderer.page, content) %>
      <% end -%>
      <%= render :partial => 'pages/contents/modules/share/content' if not @renderer.page.in_restricted_context? and not @renderer.page.parent_type == 'RegistrationForm' %>
    </article>
  </div>
</div>
