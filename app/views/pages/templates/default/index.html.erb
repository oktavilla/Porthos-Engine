<% @page_class = @renderer.layout_class -%>
<% @page_title = @renderer.title -%>
<% @page_id    = @renderer.page_id -%>
<div id="content">
  <h1><%= @renderer.title %></h2>
  <ul>
  <% @renderer.pages.each do |page| %>
    <li>
      <h2><%= page.title %></h2>
      <%= page.long_description %>
    </li>
  <% end %>
  </ul>
</div>